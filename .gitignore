.DS_Store
.env
__pycache__/
*.pyc
node_modules/
.next/ 
.next/

# Python virtualenvs
.venv/
venv/

# The Phase 1 SQLite ledger and its WAL sidecars. Local spend data — never committed,
# and it would conflict on every merge anyway.
meter.db
meter.db-wal
meter.db-shm
*.db
*.db-wal
*.db-shm

# WildChat splits — regenerate with scripts/fetch_wildchat.py. Not committed:
# ~2MB of third-party conversation text, and the test split must stay untouched
# rather than travel around in diffs.
data/wildchat/
