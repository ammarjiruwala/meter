import { MeterLoader } from "@/components/MeterLoader";

export default function Loading() {
  return (
    <MeterLoader
      title="Your Control Room"
      note="Setting up your session — the same dashboard the team uses, scoped to your calls."
    />
  );
}
