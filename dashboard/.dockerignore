# Keep the build context small and the image clean.
node_modules
.next
.git
.env
.env.local
npm-debug.log*
# The ledger never belongs in an image — the dashboard reads Postgres over the network.
*.db
*.db-wal
*.db-shm
