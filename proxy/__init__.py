"""Meter proxy package.

Run it with:

    uvicorn proxy.app:app --host 0.0.0.0 --port 8080

See ``proxy/README.md`` for the module map and the pieces that are deliberately
unimplemented in Phase 1.
"""

__version__ = "0.1.0"
